%h1 All Accounts
= link_to "Auth with Google", [:new, :account]
%table
  %tr
    / %th Email
    %th Refresh Token
  - @account.each do |account|
    %tr
      / %td= account.email
      %td= account.refresh_token
